<div class="conmment">
<table>
	<tr>
		<td><%= image_tag avatar_url(comment) %></td>
		<td><p><strong><%= comment.name %></strong><br> <%= time_ago_in_words(comment.created_at) %> Ago</p></td>
	</tr>
</table>
	
	
	
	<p><%= comment.body %></p>
	
	<p>
		<%= link_to 'Delete' ,[comment.post, comment], method: :delete, data: {confirm: 'Are you sure?'} %>
	</p>
</div>